/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['www.google.com','encrypted-tbn0.gstatic.com'], // Add the domain of the image
    },
  };
  
  export default nextConfig;
  